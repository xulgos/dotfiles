screen -h 10000 autotest 
split -v
resize -h 60
focus
screen

bind h focus left
bind j focus down
bind k focus up
bind l focus right
# C-a [, starts scroll mode, ESC to exit
# spacebar to start copy mode and spacebar again ends copy mode
